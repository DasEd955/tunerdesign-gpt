"""tests/ - Pytest test suite for the tunerdesign-gpt repository.

Covers unit and integration tests for every major module and stage in
building up the GPT: data pipeline, normalization, embeddings, attention,
transformer blocks, the full GPT model, training, inference, and the
end-to-end pipeline.
"""
